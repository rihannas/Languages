// navigating through the family hierarchy of the dom

// (parent).firstElementChild
// (parent).lastElementChild
// (child).parentElement
// (child).nextElementSibling
// (child).previousElementSibling
// (parent).children[]
// Array.from(.children) //returns a list which forEach can be used on since children doesn't return